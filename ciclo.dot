digraph G {
        user;

        inputs;
        transformations;
        state;
        merge;

        inputs -> transformations -> merge;
        inputs -> state -> merge;

        user -> inputs;
        merge -> user;
}
